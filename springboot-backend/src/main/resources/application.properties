server.port=80

# create and drop tables and sequences, loads import.sql
spring.jpa.hibernate.ddl-auto=update

# MySQL settings
spring.datasource.url=jdbc:mysql://localhost:3306/pet_adoption_system?useSSL=false
spring.datasource.username=Trainee1
spring.datasource.password=!QAZSE4
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# logging
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
logging.level.org.hibernate.SQL=debug

# JWT - pending
jwt.secret-key=pawheroadoptionsystem

### Logging ###
logging.level.org.hibernat.SQL=DEBUG
logging.level.org.springframework.web=INFO
logging.level.com.fdmgroup=DEBUG
logging.file.name=application.log